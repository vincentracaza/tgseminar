#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../tsd/deploy/tsd" "$@"
  ret=$?
else 
  node  "$basedir/../tsd/deploy/tsd" "$@"
  ret=$?
fi
exit $ret
